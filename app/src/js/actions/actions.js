import State from 'state'

let actions = {};

let actions.loadJSON = () => {
  // load JSON promise
}

let actions.setDataFetchTimer = () => {

}

let actions.generateTimeline = () => {
  // recieve new headlines and Hashtags

  // generate timeline sequence data for what should be displayed when

  // queue up timeline sequence to be played next
}

export default actions;
